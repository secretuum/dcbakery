# Координация задач по сайту DC Bakery

Файл ведёт главный терминал части («sayt-dc-bakery-main»). Код здесь не пишут — только
кто какие файлы занял, что принято и что ждёт кнопки владельца.

## Правила

- Каждая задача живёт в своей копии (`git worktree` в `.claude/worktrees/`) и своём терминале.
- Перед выдачей задачи на общий файл — `python3 ~/.config/i3/claude/overlap.py <папка части>`;
  красное («одни строки») — задачи разводим по зонам или ставим в очередь.
- Merge пул-реквестов с 18.09.2026 жмёт главный терминал части (по слову владельца).
  Деплой на сервере и пуш прямо в главную ветку — по-прежнему только рукой владельца.
  Влитое в `main` в прод само не уезжает: оно ждёт деплоя владельца.
- Запретные зоны (без личного разрешения владельца): `app/api/payments/**`, `app/api/orders/**`,
  `app/api/admin/login/**`, `app/api/whatsapp/webhook/**`, `supabase/migrations/**`,
  `src/lib/supabase/admin.ts`, `proxy.ts`, `.env*`.
- Тесты в копии: `export PATH="/home/room/.nvm/versions/node/v24.21.0/bin:$PATH"; npm test`.
  На главной ветке 21.09.2026 — 569 pass / 0 fail.

## Занятые зоны

| Задача (ветка копии) | Файлы | Статус |
|---|---|---|
| `worktree-pr20-free-delivery` | — | PR #20 влит 18.09.2026, зоны свободны; копию можно убирать |
| — | — | свободных задач нет, все копии убраны |
| `worktree-catalog-promo-import` | — | PR #29 влит 18.09.2026, зоны свободны (в том числе `package.json` и `scripts/test-resolver.mjs`) |
| `worktree-razdely-katalog-teksty` | `app/[locale]/(main)/catalog/[category]/**`, `src/lib/seo/**`, `src/i18n/*.json` | PR #49 влит 21.09.2026 (`607b795`), зоны свободны |
| `worktree-vygruzka-obnovlenie` | `scripts/i18n-export.mjs`, `docs/i18n/**`, `src/i18n/export-status*.ts` | PR #48 влит 21.09.2026 (`85d93cd`), зоны свободны |
| `worktree-vygruzka-peresborka` | `docs/i18n/**`, `scripts/i18n-export.mjs` (граница `new-since`), `src/i18n/export-status*.ts` | PR #50 влит 21.09.2026 (`5a13319`), зоны свободны |
| `worktree-npm-audit-fix` | `package.json`, `package-lock.json` (свой `node_modules` в копии, не симлинк) | выдана 21.09.2026 |
| `worktree-svg-flag` | `next.config.ts` + тест к нему | PR #52 влит 22.09.2026 (`1685f4e`), зоны свободны |
| `worktree-deliveryfee-chistka` | `app/constants.ts`, `app/api/orders/route.ts`, `app/api/admin/orders/route.ts`, `src/lib/whatsapp/orders/**`, `CheckoutForm.tsx`, `CartSummary.tsx`, `product-jsonld.ts` | выдана 21.09.2026; запретные зоны — с личного разрешения владельца, только вычистка `deliveryFee` |

21.09.2026: `overlap.py` — чисто. Словари `src/i18n/*.json` правила только `razdely-katalog-teksty`,
`docs/i18n/**` — только `vygruzka-obnovlenie`; обе влиты, пересборку выгрузки ведёт `vygruzka-peresborka`.

Зоны двух задач не пересекаются (`overlap.py` — чисто). Пока `pr20-free-delivery` занимает
`src/i18n/en.json` и `src/i18n/kk.json`, задачи по текстам (оферта 7.1/7.2, вычитка казахского)
не выдаём — сойдутся на тех же строках.

## Журнал

| Дата | Что | Ссылка |
|---|---|---|
| 15.09.2026 | Контакты: единый номер +7 747 727 2650 | PR #23 (влит) |
| 15.09.2026 | WhatsApp-ссылки: номер без «+» | PR #22 (влит) |
| 15.09.2026 | Плашка «сайт в разработке» + закрытая регистрация | PR #21 (влит) |
| 18.09.2026 | Локальная `main` перемотана на `652bc0e` (отставала на 6 коммитов), главная копия снята с влитой ветки `whatsapp-number-2650` | — |
| 18.09.2026 | Заведена задача `pr20-free-delivery` — PR #20 конфликтует с главной (i18n en/kk + фикстура телефона в `business-context.test.ts`) | PR #20 (открыт) |
| 18.09.2026 | Заведена задача `catalog-promo-import` — работа из `catalog-promo` (импорт прайсов) идёт в PR | — |
| 18.09.2026 | Починен ssh: в `~/.ssh/config` добавлен хост `github-dcbakery` с ключом `~/.ssh/windows/dcbakery_deploy` (по слову владельца). `git fetch/push` работает | — |
| 18.09.2026 | Из `.claude/settings.local.json` снят `mcp__claude_ai_Supabase__execute_sql` и выброшены мёртвые виндовые правила; чтение базы (list_projects/list_tables/get_logs) осталось свободным | — |
| 18.09.2026 | Заявка [13137] `pr20-free-delivery` принята: главная слита в PR #20 (merge-коммит `9f2764e`), конфликты en/kk + фикстура телефона разрешены, ветка `free-delivery-min-15k` перемотана без перезаписи | PR #20 |
| 18.09.2026 | PR #20 влит в главную (`25e734f`) — бесплатная доставка по Алматы на все заказы, минимум заказа 15 000 ₸, новый текст оферты 7.1/7.2. Проверки Netlify зелёные, тесты на главной 269 pass / 0 fail | PR #20 (влит) |
| 18.09.2026 | PR #24 влит (`36891eb`) — журнал координации в репозитории | PR #24 (влит) |
| 18.09.2026 | **SEO, три задачи сданы и влиты.** PR #34 (`9f6ccb1`) — карточки товаров и разделы каталога стали настоящими ссылками: на превью в HTML `/ru/catalog` появились 47 ссылок на товары и 3 на разделы, было ноль и ноль; обычный клик по-прежнему даёт быстрый просмотр, Enter и Ctrl+клик ведут на страницу. PR #35 (`c4e9a5f`) — чек-лист владельца `docs/seo/chek-list-vladeltsa.md` и переписанный пак карточек. PR #36 (`952e9e6`) — починен `og:image` (был `https://dc-bakery.kzhttps//…`), свой Open Graph на каждой странице, `ItemList`, полный `Product`, фолбэк описания. Тесты на главной 319 pass / 0 fail (было 284) | PR #34, #35, #36 |
| 18.09.2026 | **Безопасность.** Заявка [23367] `next-image-fix` принята, PR #31 влит (`79d2e79`). Закрыт неавторизованный путь: `remotePatterns` держал шаблон `*.supabase.co`, а Next сверяет хост через picomatch — под `*` подходил ЛЮБОЙ чужой проект Supabase; `/_next/image` публичный (matcher в `proxy.ts:322` исключает `_next/`), то есть аноним заставлял наш сервер скачать свой файл и разобрать его нативным декодером. Теперь хост берётся только из `NEXT_PUBLIC_SUPABASE_URL`. Плюс `next` 16.2.9 → 16.3.5 (critical GHSA-2xp9-vwfh-vxw4, RCE через AVIF), `sharp` 0.34.5 → 0.35.4, libvips 1.2.4 → 1.3.3. Третья правка — снят `experimental.rootParams`: в 16.3.5 его нет в типах. Три правки НЕРАЗДЕЛИМЫ: 16.2.9 без флага не собирается вовсе (`next/root-params` нельзя импортировать), 16.3.5 с флагом падает на TS2353. Проверено главным: тесты, `tsc`, аудит по lock, отсутствие флага в типах пакета; независимая сборка — превью-деплой PR с установкой зависимостей с нуля | PR #31 (влит) |
| 18.09.2026 | Заявка [16931] `catalog-promo-import` принята, PR #29 влит (`a93f7bb`): `jszip` объявлен в зависимостях (+ строка в lock, иначе `npm ci` на деплое падает с EUSAGE), мёртвый «Шаг» убран из разбора прайса и выгрузки, добавлено 15 тестов на разбор прайса. Проверено главным на слитом дереве: 284 pass / 0 fail, `tsc --noEmit` чисто, `npm ci --dry-run` проходит. Выгрузка каталога теперь 13 колонок вместо 14; старые файлы с «Шагом» читаются по-прежнему (колонки ищутся по заголовку) | PR #29 (влит) |
| 18.09.2026 | **Деплой владельцем**: прод обновлён до `fdb6e2c` (`/opt/dcbakery/deploy.sh` на 82.115.43.151). Проверено главным: `/ru`, `/ru/catalog`, `/ru/oplata-i-dostavka`, `/ru/oferta`, `/ru/cart`, `/api/ping` — 200; новый пункт 7.2 оферты на проде; упоминаний старых тарифов «1 500»/«3 000» — ноль | — |
| 18.09.2026 | Ветка `catalog-promo` закрыта как поглощённая главной: patch-id совпадает (`2711084` ≡ `a0b55c9`, `6f29378` ≡ `46f5d9c`). Сухое слияние меняет только `app/api/admin/products/promo/apply/route.ts`, и версия ветки там **старее** — в главной поверх лёг PR #13 (`pricesJson`, `classify()`). Влить ветку = откатить PR #13, поэтому PR по ней не оформляем. Ветку не удаляем | — |
| 21.09.2026 | Заявка [69284] `vygruzka-obnovlenie` принята: пропуск в kk — статус «НЕТ КАЗАХСКОГО ПЕРЕВОДА» («кг»), пропуск в en — тихая отметка (6 строк); `excluded.json` → `cleared` (все 10 удалены в `3d209a8`, не в `d186ebd`); выгрузка 884 строки. Проверено главным поверх `531ddb8`: 538 pass / 0 fail, `tsc` чисто, повторная сборка без изменений | PR #48 (влит) |
| 21.09.2026 | Заявка [69558] `razdely-katalog-teksty` принята: тексты трёх разделов в `src/lib/seo/category-texts.ts` (заголовки с «оптом»/«Алматы», описания 150–160, блок фактов), сторожа видят `/catalog/[category]`, порог 150 только для разделов; перевод заголовка каталога — полуфабрикаты вместо «мяса» в en/kk. Черновые kk/en перечислены в PR. Проверено главным поверх `531ddb8`: 558 pass / 0 fail, `tsc` чисто; вместе с PR #48 без конфликтов, 567 pass. Заявка легла на путь копии, а не части (второй раз за день — ошибка сдачи из копии) | PR #49 (влит) |
| 21.09.2026 | PR #48 (`85d93cd`) и PR #49 (`607b795`) влиты главным. На главной 567 pass / 0 fail, `tsc` чисто. Заведена задача `vygruzka-peresborka` — пересобрать выгрузку для носителя с новыми строками разделов | PR #48, #49 (влиты) |
| 21.09.2026 | По слову владельца: отмашка главного терминала = ответ владельца. Вписано в `FIRST_TASK` (`~/.config/i3/claude/cc-launch.py`) и в `team.md`; действует в окнах, открытых после правки. Открывает только работу по задаче в своей зоне | — |
| 21.09.2026 | **Деплой владельцем** до `607b795`. Проверено главным на проде: `/ru/catalog/deserty` — «Десерты оптом в Алматы для кофеен и HoReCa | DC Bakery» и блок «Коротко о разделе»; `/en/catalog` — «desserts, frozen foods» вместо «meat» | — |
| 21.09.2026 | Заявка [70565] `vygruzka-peresborka` возвращена: статусы «НОВОЕ с 18.09.2026» зависят от часа запуска — `scripts/i18n-export.mjs:170` зовёт `git rev-list --before=2026-09-18`, а git читает дату без времени как текущее время суток 18.09. Перезапуск главным на том же коммите дал 19 строк с другим статусом. Зона расширена на эту строку скрипта; граница — 00:00 +05:00 | — |
| 21.09.2026 | Заявка [70565] принята со второго захода, PR #50 влит (`5a13319`): выгрузка 898 строк, граница «НОВОЕ» — `newSinceBoundary()` = полночь +05:00. Проверено главным прогоном в 11:46 (после коммитов 18.09): 42 «НОВОЕ», `git status` чистый; 569 pass / 0 fail, `tsc` чисто. Комплект для носителя — `docs/i18n/strings-translated.md` | PR #50 (влит) |
| 22.09.2026 | Заявка [74587] `svg-flag` принята, PR #52 влит (`1685f4e`): снят `images.dangerouslyAllowSVG` и парные `contentDispositionType`/`contentSecurityPolicy` (равны умолчаниям Next), добавлен сторож `src/lib/next-config.test.ts`. Проверено главным на слитом дереве: 570 pass / 0 fail, `tsc` чисто, SVG через `<Image>` в коде нет. В основном чекауте сделан `npm ci` — стоял next 16.2.9 при 16.3.5 в `package.json` | PR #52 (влит) |
| 22.09.2026 | PR #53 влит (`2829a65`) — документы: решения владельца 21.09, таблица выданных задач, отметка «вариант А» в чек-листе | PR #53 (влит) |
| 22.09.2026 | Выгрузка Google Search Console (данные по 18.09): в индексе 42 страницы из 174, 124 — «обнаружена, не проиндексирована», у всех дата сканирования `1970-01-01`, то есть Googlebot их ни разу не открывал; поровну ru/kk/en, значит дело в очерёдности обхода, а не в языке. 5 — «просканирована, но пока не проиндексирована» (`/ru/optom` 19.09, `/kk/optom`, 3 товара), 3 — переадресации (`/`, `/ru/`, `/en/`), это норма. Страницы на проде проверены: `index, follow`, canonical на себя, 270–730 слов. Задачу по коду не заводили: чинить нечего, нужны внешние ссылки и время | — |
| 24.09.2026 | **Прод лежал в 502.** `pm2 list` — `online`, но в логе по кругу `Could not find a production build in the '.next' directory`, счётчик перезапусков 362. Места и памяти хватало, записей OOM нет. Причина: `deploy.sh` (`git pull` → `npm ci` → `npm run build` → `pm2 reload`) оборвался посередине — сборка `.next` не доделалась, а код уже перемотан на `2829a65`. Лечение владельцем по шагам главного: `pm2 stop` → `npm run build` → `pm2 restart` → `pm2 save`. Сайт поднят, прод теперь на `2829a65` — то есть PR #52 и #53 фактически задеплоены. Проверено главным снаружи: `/ru`, `/ru/catalog`, `/ru/optom`, `/api/ping`, `sitemap.xml` — 200; в каталоге 110 ссылок на товары и 3 на разделы; `/_next/image` отдаёт webp (200), а SVG теперь отклоняет (400) — эффект PR #52 виден на живом сайте | — |
| 24.09.2026 | `docs/deploy/vps-almaty.md`: новый раздел «Безопасный деплой». Скрипт откладывает рабочую `.next` в `.next.prev`, после сборки ждёт `200` от `/api/ping` (10 попыток по 3 с) и при любой ошибке, обрыве (`ERR INT TERM HUP`) или молчании сайта возвращает прежний коммит и прежнюю сборку. Запуск под `tmux` с логом в `/var/log/dcbakery-deploy.log`. Проверено главным на песочнице (заглушки npm/pm2/curl, игрушечный репозиторий), 4 сценария: удачный деплой, падение сборки, сборка прошла — сайт молчит, обрыв сигналом HUP. Во всех трёх плохих — откат кода и сборки на прежний коммит. Ставит скрипт на сервер владелец | — |

## SEO, 18.09.2026 — найдено и в работе

- **Страницы товаров и категорий — сироты**: в HTML каталога ноль ссылок `/{locale}/product/…`
  и ноль ссылок `/{locale}/catalog/<категория>`. Карточка — `<button>`, навигация по категориям —
  кнопки со `scrollIntoView`. 141 адрес товаров и 9 адресов категорий есть в карте сайта, но
  внутренних ссылок на них нет: вес не передаётся, обход только по sitemap, не работают средняя
  кнопка мыши и клавиатура. Чинит задача `seo-ssylki-na-tovary`: карточка становится настоящей
  ссылкой, клик по-прежнему открывает быстрый просмотр (решение владельца), Enter ведёт на страницу.
- **Битый `og:image` у всех товаров с фото из админки**: на проде отдаётся
  `https://dc-bakery.kzhttps//<проект>.supabase.co/...` — абсолютный URL Supabase склеивается с
  адресом сайта, двоеточие съедается нормализацией пути. То же в `"image"` у JSON-LD. Превью ссылок
  на товары в мессенджерах сломано, а менеджеры рассылают клиентам именно такие ссылки.
  Чинит задача `seo-metadannye-razmetka`, первым пунктом.
- **og-теги наследуются от layout**: страницы без своего `openGraph` отдают общесайтовые
  `og:title`/`og:description`, а `og:url` — корень локали. Ссылка на категорию в мессенджере
  выглядит как ссылка на главную. Там же: `ItemList` нет нигде, `Product` без `sku` и
  `priceValidUntil`, у товара нет фолбэка описания.
- **Внешнего присутствия нет вовсе**: поиск по «dc-bakery.kz», «DC Bakery Алматы оптом десерты»,
  «DC Bakery 2ГИС» не даёт ни сайта, ни карточки компании — ни Google Business Profile, ни 2ГИС,
  ни Яндекс.Бизнес, при том что родственный Del Cappuccino в 2ГИС представлен. Для локального B2B
  это рычаг сильнее любой правки кода. Задача `seo-vneshnie-kartochki` готовит владельцу чек-лист
  с готовыми текстами.
- **Ложная тревога, снято**: `alt` у фотографий товаров в порядке — 55 картинок с названиями
  товаров, пустых две (пиксель Метрики и логотип), и там пустой `alt` правильный.
- **Отложено отдельной задачей** (пункты E и G из аудита): описания за обрезкой (`/optom`
  205–265 символов, главная 176 kk / 163 en), слабый `title` у `/catalog` без «оптом» и «Алматы»,
  отсутствие `lastmod` у статических страниц и разделов. Там новые тексты и черновой kk/en —
  принимать отдельным диффом, чтобы по нему же собрать вычитку носителя.
- **Черновые переводы, ждут носителя**: «Открыть страницу товара» (PR #34) и запасное описание
  товара «${name} — оптовая поставка от DC Bakery в Алматы…» (PR #36) — обе строки в `kk.json`
  и `en.json`.

## Найдено в главной 18.09.2026 (задача `catalog-promo-import`, проверено главным)

- **`jszip` — необъявленная зависимость.** `src/lib/catalog-import.ts:3` импортирует `jszip`, в
  `package.json` его нет: работает транзитивно из `exceljs ^3.10.1` (стоит 3.10.1). Обновится
  `exceljs` — fallback-чтение прайса отвалится на сборке. Починка в работе.
- **Разбор прайса не покрыт тестами**: `parseCatalogWorkbook`, `readOoxmlCells`, `extractFileTable`
  не упоминает ни один тест — это ровно тот код, который в августе падал с «Файл пустой или не
  распознан». Тесты в работе.
- **`step_qty` мёртв по всему проекту** — разобрано 18.09.2026, поведение НЕ меняем, решение за
  владельцем. Поле разбирается из прайса, но не доезжает никуда: `src/lib/catalog.ts` строит
  `Product` в трёх местах (:142, :216, :276) и везде ставит литерал `step_qty: 1` — в `applyOverride`
  (:216) соседние `min_qty`, `price`, `stock_qty` читаются из оверрайда, а шаг нет; ручной редактор
  админки `app/(unlocalized)/admin/products/actions.ts:112` тоже пишет `step_qty: 1`; выгрузка
  `src/lib/catalog-xlsx.ts:35` отдаёт колонку «Шаг» из всегда-единицы; `rowPatch` его не применяет.
  `override.step_qty` не читает никто. Колонка в базе при этом есть
  (`supabase/migrations/202606230006_expand_catalog_product_overrides.sql:27`), а
  `upsertCatalogProductOverride` пишет патч без белого списка — то есть запись прошла бы и легла
  мёртвым грузом, а предпросмотр импорта обещал бы админу изменение, которого система не делает.
  Поэтому «просто применить в `rowPatch`» — хуже, чем сейчас (это моё раннее указание, снято).
  **Решение владельца 18.09.2026: кратность (продажа коробками) не нужна — убираем мёртвое.**
  Задача `catalog-promo-import` убирает `step_qty` из `CatalogFileRow` и разбора, правит обещание в
  комментарии `catalog-import.ts:9` и снимает колонку «Шаг» с выгрузки `catalog-xlsx.ts`. Не трогаем:
  тип оверрайда в `src/lib/supabase/admin.ts` (запретная зона), колонку в базе и миграции, а также
  литералы `step_qty: 1` в `catalog.ts` и `actions.ts` — поле есть в типе `Product` и его читает
  корзина, единица там и должна быть. Убираем ложное обещание импорта, а не само поле.

## Решено владельцем 18.09.2026

- **PR #20**: бизнес-условие остаётся как в ветке — минимум заказа 15 000 ₸ и бесплатная доставка.
- **`catalog-promo`**: доводить оказалось нечего — ветка уже в главной (см. журнал). Задача
  `catalog-promo-import` переведена на то, что нашла в главной: тесты на разбор прайса и `jszip`.
- **ssh** и **права на SQL**: починено и вычищено, см. журнал.

## Не доделано, факты собраны — забирать отсюда

Две задачи 20.09.2026 закрыты не начатыми: они простояли на паузе (первая реплика окна задачи
запрещает менять файлы до ответа пользователя, и снять это может только он в том же окне).
Разведку обе сделали, выводы ниже — повторять её не нужно.

**Доводка трёх страниц разделов каталога.** Состояние: заголовки без слов «оптом» и «Алматы»
(«Десерты | Каталог DC Bakery»), описания 69/71/79 символов при норме 150–160, интро-абзацы
есть и приличные. Что выяснилось сверх задания:
- **сторожа эту страницу НЕ проверяют**: `/catalog/[category]` исключён через `DATA_DRIVEN_TITLES`
  в `src/lib/seo/page-sources.ts:162`, а проверка длины ищет литерал `const description = t("…")`
  (`DESCRIPTION_KEY`, строка 85), которого там нет — описание берётся из `currentCategory`.
  Чтобы сторожа заработали, тексты надо вынести туда, где их видит разбор исходника;
- **у сторожа длины нет нижней границы вообще** — только потолок 160. Глобальный минимум 150
  красит пять чужих страниц: главная 146/159/153, оферта 149/145/153, оплата-доставка 134/138/129,
  контакты 111/115/120, приватность 111/120/125 (ru/kk/en). Решение: минимум вводить только для
  разделов каталога. Для остальных — отдельная задача и разбор по типу страницы: коммерческим
  дотянуть, служебным (контакты, приватность) оставить как есть;
- **данные товаров**: десерты 34 позиции, 9 подгрупп, коробка/банка, +2…+8 °C и +2…+4 °C, сроки
  72 ч / 3 / 5 / 10 суток, у части позиций `shelfLife: "Уточнить"` — нужна вилка с оговоркой;
  полуфабрикаты 11 позиций, вакуум, −18 °C, 6 мес.; мясо 4 позиции, вакуум, −18 °C, 12 и 6 мес.
  Халал стоит у товаров всех трёх разделов;
- **ошибка в переводе, не исправлена**: ключ «Каталог оптом, Алматы — десерты, полуфабрикаты»
  переведён на en как «desserts, meat» и на kk как «десерттер, ет» — в обоих «мясо» вместо
  «полуфабрикатов». Это заголовок главной коммерческой страницы.

**Пересборка выгрузки для носителя.** Выяснено:
- в словарях 884 ключа, в выгрузке 883 — не хватает «Из этого же раздела» (PR #45);
- **правило пометок дырявое**: статус «НЕТ В СЛОВАРЕ» ставится только если строки нет в ОБОИХ
  словарях (`scripts/i18n-export.mjs:132`). Односторонние пропуски проходят молча: «кг» есть в
  `en.json` и отсутствует в `kk.json` — уедет к носителю пустой клеткой без предупреждения.
  Обратная сторона: шесть ключей есть в kk и нет в en («...», «…», «Собственное производство»,
  «Пекарня полного цикла», «DC Bakery — B2B поставщик в Алматы» и ещё один);
- решено: казахские пропуски помечать громко отдельным статусом, английские — тихо, отметкой в
  колонке «English», чтобы не размывать приоритеты (комплект для казахского носителя);
- `docs/i18n/excluded.json` стал холостым: все 10 записей описывают строки, вычищенные из словарей
  в `d186ebd` и `3d209a8`. Решено переносить их под ключ «cleared» с датой и ссылками, а не удалять;
- `highlight_since` оставляем `2026-09-18`: носитель ещё не смотрел ни одну строку, граница
  «до него / после него» появится, когда он вернёт первый файл. Дата в шапке — фактическая.

## Висит на владельце

Пересмотрено 21.09.2026.

- **Внешние карточки** — `docs/seo/chek-list-vladeltsa.md`, 11 шагов, первые пять дают почти всё:
  карта сайта в вебмастера, Google Business Profile, 2ГИС, Яндекс.Бизнес, Bing Places. Руками владельца.
- **Выгрузка для носителя** — отдать `docs/i18n/strings-translated.md`, начинать с 42 строк «НОВОЕ с 18.09.2026».

## Решено владельцем 21.09.2026

- **Карточка в справочниках — самостоятельная «DC Bakery»** (вариант А чек-листа), не подразделение
  Del Cappuccino. Свой телефон +7 747 727 2650, в описании «by Del Cappuccino».
- **Срок доставки в разметке — не указываем**, оферта не меняется. Пустые `deliveryTime`,
  `handlingTime`, `transitTime` и поля окна возврата остаются пустыми намеренно (сроки по п. 7.1/7.3
  согласуются при заказе; возврат по п. 9.1 — `MerchantReturnNotPermitted`). Вопрос закрыт.
- **`exceljs` + `uuid` в `npm audit`** — не трогаем, ждём исправления выше по течению: единственное
  «исправление» npm — откат `exceljs` на 3.4.0, он рискованнее самой уязвимости (файлы грузит только админ).
- **`deliveryFee()`** — вычищаем, с разрешением на запретные зоны только под эту правку
  (задача `deliveryfee-chistka`).
- Закрыто как сделанное: деплой (прод на `607b795`, проверено 21.09); `public/llms.txt` и `/optom`
  больше не обещают «банкетные десерты» и «минимального заказа нет» (`4df49f0`, 19.09).
- В работе: остаток `npm audit` без смены мажоров (`npm-audit-fix`). Снятие `dangerouslyAllowSVG`
  (`svg-flag`) влито 22.09.2026, PR #52.
- 22.09.2026: владелец поручил главному терминалу мержить PR самому; деплой — по-прежнему владелец.
  В основном чекауте сделан `npm ci` — `node_modules/next` был 16.2.9 при 16.3.5 в `package.json`.

## Архив: висело на владельце до 21.09.2026

- **Деплой** — в главной накопились ссылки на товары и починенные превью ссылок. Прод на
  `79d2e79`, главная на `952e9e6`. Команда прежняя: `sudo -i`, затем
  `cd /opt/dcbakery && ./deploy.sh`. После деплоя проверить превью ссылки на товар в WhatsApp.
- **Внешние карточки** — `docs/seo/chek-list-vladeltsa.md`, 11 шагов. Первые пять дают почти всё:
  карта сайта в вебмастера, Google Business Profile, 2ГИС, Яндекс.Бизнес, Bing Places.
- **Решение по карточке**: DC Bakery — самостоятельная карточка или подразделение Del Cappuccino.
- **Текст для разметки возврата и доставки**: `hasMerchantReturnPolicy` и `shippingDetails` в
  `Offer` не заполнены намеренно — они обязаны повторять оферту слово в слово. Нужны формулировки
  владельца, после этого мелкая задача.
- **`public/llms.txt` и тексты `/optom` расходятся с реальностью**: обещают «банкетные десерты»
  (категория закрыта флагом) и «минимального заказа нет» (на деле 15 000 ₸). Это публичный текст,
  который читают ИИ-поисковики. Нужна отдельная задача, формулировки — за владельцем.

- **Деплой сделан 18.09.2026**: прод на `79d2e79`. Проверено вживую: страницы 200, фото товаров
  с `vdydhptmfhnrjrtqxktw.supabase.co` отдаются 200, чужой проект Supabase получает 400 за 0.18 с,
  посторонний домен — 400. Старых тарифов доставки на странице оплаты нет, оферта 7.2 в новой
  редакции. Команда деплоя на будущее: `sudo -i`, затем `cd /opt/dcbakery && ./deploy.sh`.

- **Остаток по аудиту после PR #31**: было 11 записей (4 moderate / 6 high / 1 critical), стало
  **10 и ноль critical**, `sharp` ушёл из списка. Отдельного документа не делаем — считается
  командой `npm audit` за секунду. Что осталось и требует решения владельца:
  **`exceljs` + `uuid`** (обе moderate) — npm предлагает `exceljs@3.4.0`, а это не апгрейд, а
  ОТКАТ с нашей `^4.4.0`: исправленной версии в четвёртой ветке нет. Именно этот код разбирает
  загруженный прайс, ради него написан ручной OOXML-fallback. Откат рискованнее самой уязвимости
  (файлы грузит только админ) — предложение: не трогать, ждать исправления выше по течению.
  Остальные шесть (`@tailwindcss/postcss`, `browserslist`, `js-yaml`, `nanoid`, `brace-expansion`,
  `baseline-browser-mapping`) чинятся без смены мажоров, но висят на tailwind и postcss.
- **Мёртвый флаг `dangerouslyAllowSVG`** в `next.config.ts`: разбор показал, что он не нужен —
  SVG-заглушки товаров перехватывает `FallbackImage` до оптимизатора, иконки категорий идут мимо
  него обычным `img`, а загрузка в админке валидирует magic bytes и SVG не пускает. Выключение по
  коду ничего не ломает. Отдельной мелкой правкой, не срочно.
- **Языки** — порядок с 18.09.2026: черновой перевод ru/en/kk делаем сами, задачи из-за языка
  не останавливаем. Носитель вычитывает потом, владелец передаёт поправки. В описании PR черновые
  строки помечать отдельно, чтобы вычитку было по чему собрать.
- **Мелкий хвост от PR #20**: `deliveryFee(subtotal?)` в `app/constants.ts` теперь всегда
  возвращает 0, параметр оставлен для совместимости с `eslint-disable`. Работает, но при случае
  стоит вычистить вызовы — отдельной мелкой задачей, не срочно.
