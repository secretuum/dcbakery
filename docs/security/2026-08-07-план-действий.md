# План действий: безопасность и баги аудита (для разработчика)

_07.08.2026. Пошаговый чек-лист по всем находкам. Детальные коды/SQL — в соседних файлах `docs/security/`. Отмечай `[x]` по мере выполнения._

## Уже сделано (контекст)

- [x] **Затычка #1/#2 применена на проде** (`REVOKE select/insert/update/delete FROM anon, authenticated` на 6 таблицах + `ENABLE RLS magic_link_tokens`). Утечка чтения закрыта. **НО** это ручной SQL — не переживёт `db reset`, нужна миграция (Шаг 1).
- [x] **#4/#5/#6 реализованы в коде** (гейты зелёные, 6 файлов уникоммичено). Осталось закоммитить (Шаг 2).

---

## ПРИОРИТЕТ 1 — Безопасность данных

### Шаг 1. Постоянная миграция RLS/грантов (#1, #2)

Затычка временная. Зафиксируй её миграцией.

1. [ ] Создай файл `supabase/migrations/<timestamp>_harden_data_access.sql`. Содержимое — из **`docs/security/2026-08-07-rls-hardening.md`, раздел «Шаг 2»** (`ENABLE RLS` на `magic_link_tokens` + `REVOKE ALL/…` на 6 таблиц). Рекомендую `REVOKE ALL` (уберёт и оставшиеся `TRUNCATE/REFERENCES/TRIGGER` — они через PostgREST недостижимы, но для чистоты).
2. [ ] Применить: `supabase db push` (или твой обычный процесс). Прод уже закрыт затычкой — миграция просто приведёт схему в соответствие; убедись, что не конфликтует (idempotent: `enable rls`/`revoke` повторно безопасны).
3. [ ] Проверка (SQL Editor): у `anon`/`authenticated` нет `SELECT` на `orders/clients/whatsapp_clients/magic_link_tokens/order_items/payment_events`; `magic_link_tokens.relrowsecurity = true`.
4. [ ] Смоук: вход по коду (magic-link), регистрация клиента, оформление заказа, админка — всё работает (приложение ходит через `service_role`, не задето).

### Шаг 1б. Хэш magic-link токена (defense-in-depth, отдельный commit)

Сейчас токен входа хранится сырым. Даже при закрытом RLS — если строка когда-то утечёт (лог, бэкап), это рабочий ключ.

1. [ ] Миграция: заменить колонку `token` на `token_hash` в `magic_link_tokens`.
2. [ ] `src/lib/magic-link-store.ts`: при записи хранить `sha256(token)`, при сверке искать по `sha256(token)`. Образец уже есть — `src/lib/registration/reg-link.ts` (`createHash('sha256')...`, колонка `token_hash`).
3. [ ] Проверка: вход по коду работает; в таблице лежат хэши, не сырые токены.

---

## ПРИОРИТЕТ 2 — Закоммитить готовые фиксы #4/#5/#6

Уже в рабочем дереве, гейты зелёные (tsc/lint + 164 теста). 6 файлов:
`src/lib/credit.ts`, `src/lib/orders/actions.ts`, `app/api/whatsapp/webhook/route.ts`, `app/api/orders/route.ts`, `src/lib/whatsapp/orders/orchestrator/orchestrator.ts`, `…/orchestrator.test.ts`.

1. [ ] Ревью diff (особенно реструктуризация `orchestrator.ts` — лок до dedup).
2. [ ] `npx tsc --noEmit && npm run lint && npm test` (для контроля).
3. [ ] Коммит.

> **#5 — помни про оговорку:** реализован МЯГКИЙ сигнал (⚠️-пометка менеджеру + флаг `requiresPrepay` в ответе). Жёсткий стоп — см. Приоритет 4.

---

## ПРИОРИТЕТ 3 — #3 IDOR документов (подписанный док-токен)

Полный план с готовым кодом — **`docs/security/2026-08-07-document-token-plan.md`**. Применять **АТОМАРНО одним PR** (иначе действующие ссылки в WhatsApp/Telegram сломаются).

1. [ ] Задать env `DOCUMENT_TOKEN_SECRET` на Render (32+ случайных байт) — **до** деплоя (роуты документов иначе fail-fast в проде).
2. [ ] Создать `src/lib/document-token.ts` (код готов в плане, раздел 1).
3. [ ] Enforcement в 6 роутах документов — 3 `.../xlsx/route.ts` + 3 `.../[orderId]/page.tsx` (раздел 2): нет валидного `?t=` → `404`/`notFound()`.
4. [ ] Проставить `?t=signDocumentToken(order.id)` во всех источниках ссылок (раздел 3, таблица из 9 мест). ⚠️ `orders/actions.ts` и `telegram/accountant.ts` — order-флоу, внимательно. `ProfileClient.tsx` — токен считать на сервере и прокинуть пропом.
5. [ ] Добавить `Referrer-Policy: no-referrer` на ответы документов (чтобы токен не тёк через реферер).
6. [ ] Тест `src/lib/document-token.test.ts` (раздел 5): валидный/чужой/истёкший/битый.
7. [ ] Переходный период (по желанию): роут временно принимает и старые ссылки без `t` с логом, потом фолбэк убрать — иначе уже разосланные счета отдадут 404.

---

## ПРИОРИТЕТ 4 — #5 хард-блок (сильнее мягкого сигнала)

Только если нужен технический запрет отгрузки консигнационному клиенту (`credit_limit>0`), временно превысившему лимит. Требует схемы — поэтому отдельно.

1. [ ] Миграция: пер-заказный флаг `requires_prepay boolean default false` на `orders`.
2. [ ] `app/api/orders/route.ts`: при `creditCheck.requiresPrepay` писать `requires_prepay: true` в заказ (уже вычисляется, сейчас только в `comment`).
3. [ ] `src/lib/account/tier.ts` `shipmentBlockedForPrepay`: блокировать и когда `order.requires_prepay === true` (не только `credit_limit===0`). Добавь вход `requiresPrepay` в `ShipmentGuardInput`, обнови `actions.ts:347`.
4. [ ] Тесты в `tier.test.ts` на новый случай.
> Почему не сделал сразу: шлюз намеренно ключует на статичный `credit_limit` во избежание циркулярности (перевод в `in_progress` сам добавляет долг) — коммент `tier.ts:49-58`. Пер-заказный флаг эту циркулярность обходит.

---

## ПРИОРИТЕТ 5 — Надёжность 🟡 (короткие, безопасные; можно частями)

Быстрые и низкорисковые — рекомендую сделать заодно:

1. [x] **`timingSafeEqual` в 1c-экспорте и cron.** ✅ Сделано: общий хелпер `src/lib/http/timing-safe.ts` (`timingSafeCompare`, +тест), применён в `1c/orders/route.ts` и `cron/overdue/route.ts`.
2. [x] **Демо-платёж: убрать захардкоженный fallback-секрет.** ✅ Сделано: `getDemoPaymentSecret` теперь fail-fast (throw), если `PAYMENT_MODE=demo` и секрет не задан; захардкоженный `'dc-bakery-local-demo'` убран.
3. [x] **Green API `sendText` без ретрая → молча `null`.** ✅ Сделано: 1 ретрай с паузой 400мс в `green-api.ts`; `send` пробрасывает результат (тип стал `Promise<string|null>`); на создании заказа при неудаче доставки — эскалация менеджеру (`notifyManager`), +тест.

Средние — ✅ ВСЕ СДЕЛАНЫ (07.08.2026, уникоммичено, 169 тестов):

4. [x] **Вложение от клиента больше не теряется.** Ветка `unsupported` в `orchestrator.ts` теперь заводит лид (`createLead`) + зовёт менеджера (`notifyManager`), а не только отвечает отказом.
5. [x] **Prompt-injection: эскалация на ЖЁСТКОМ подмножестве.** `injection.ts` разделён на `HARD_INJECTION_PATTERNS` (ignore instructions/system prompt/sql/script/path — низкий FP) и `SOFT_LINK_PATTERNS` (ссылки/curl — часто ложные, ссылка-карта в адресе). На `scan.hardInjection` бот отвечает штатно (цена серверная), но шлёт heads-up менеджеру. Мягкие/ценовые — как было, только лог.
6. [x] **Мёртвый `intent-extractor` удалён** (нигде не импортился — проверено грепом).
7. [x] **Фичефлаги читаются после дешёвых отсевов** в `webhook/route.ts` (тип вебхука, чужие @g.us, пустой chatId) — игнорируемые вебхуки больше не бьют в Supabase.

---

## Порядок, если коротко

1. Шаг 1 (миграция RLS) — **сегодня**, закрепить затычку.
2. Приоритет 2 — закоммитить #4/#5/#6.
3. Шаг 1б (хэш токена) + Приоритет 5 п.1–2 (timing-safe, демо-секрет) — быстрые, в ту же неделю.
4. Приоритет 3 (#3 док-токен) — отдельный атомарный PR.
5. Приоритет 4 (#5 хард-блок) и остальная надёжность — по мере приоритетов бизнеса.
